{
  "id": "greenfield-source",
  "entry_skill": "e2e-testing",
  "mode": "generate",
  "autonomy": {"mode": "explicit", "auto_repair": false},
  "prompt": "Use the e2e-testing Agent Skill in generate mode to create E2E coverage for checkout. Work autonomously within the repository and stop at the first durable outcome.",
  "fixture": "greenfield-source",
  "expect": {
    "manifest_status": "generated-unverified",
    "required_journey_ids": ["journey-checkout"],
    "journey_traceability": ["journey-checkout"],
    "required_evidence_ids": ["evidence-source"],
    "unchanged_globs": ["package.json", "src/**", "docs/**"],
    "present_globs": ["tests/**/*.spec.*"]
  }
}
