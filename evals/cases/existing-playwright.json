{
  "id": "existing-playwright",
  "entry_skill": "e2e-testing",
  "mode": "generate",
  "prompt": "Use the e2e-testing Agent Skill in generate mode to add checkout coverage to the existing Playwright suite while preserving its custom fixture API.",
  "fixture": "existing-playwright",
  "expect": {
    "manifest_status": "generated-unverified",
    "required_journey_ids": ["journey-checkout"],
    "journey_traceability": ["journey-checkout"],
    "required_evidence_ids": ["evidence-existing-suite"],
    "unchanged_globs": ["package.json", "playwright.config.ts", "tests/fixtures.ts", "tests/login.spec.ts"],
    "present_globs": ["tests/**/*.spec.*"]
  }
}
