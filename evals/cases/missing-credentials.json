{
  "id": "missing-credentials",
  "entry_skill": "e2e-testing",
  "mode": "verify",
  "autonomy": {"mode": "explicit", "auto_repair": false},
  "prompt": "Use the e2e-testing Agent Skill in verify mode for the configured staging target. Do not invent E2E_USER_TOKEN; record the authorization requirement.",
  "fixture": "protected-target",
  "expect": {
    "manifest_status": "needs-authorization",
    "required_evidence_ids": ["evidence-authorization"],
    "required_journey_ids": ["journey-checkout"],
    "required_next_action_ids": ["action-provide-e2e-user-token"],
    "unchanged_globs": ["package.json", "e2e-target.json", "tests/**"]
  }
}
